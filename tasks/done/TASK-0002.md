# TASK-0002: Add routine CI and manual GitHub Pages deployment path

* Status: done
* Risk: high
* Execution profile: cloud-deep
* Reasoning target: high
* Required review: independent-cloud-deep
* Security impact: material
* Security rationale: This task changes GitHub Actions identities, workflow permissions, deployment mechanics, and the release trust path for GitHub Pages.
* Next handoff: human PR integration decision; production release remains separate
* Related requirements: NFR-003, NFR-007
* Related ADRs: ADR-002

## Goal

Materialize the approved GitHub delivery baseline so that routine pull-request/main CI executes the repository's canonical verification contract on a clean GitHub-hosted runner, and a separate human-triggered workflow can build a newly verified static artifact from `main` and deploy that exact artifact to GitHub Pages with least privilege.

## Non-goals

* Do not implement product features, city/time-zone behavior, persistence, presentation settings, or alarms.
* Do not automatically deploy on push, pull request, merge, schedule, or CI success.
* Do not trigger or authorize the first production release as part of implementation or review.
* Do not change the approved GitHub Pages hosting decision or `/MiniJSClock/` production base path.
* Do not introduce a PAT, repository secret, GitHub App credential, cloud credential, or other deployment secret.
* Do not use `configure-pages` automatic enablement or broaden permissions to make Pages setup work.
* Do not add Dependabot, CodeQL, SBOM generation, artifact attestation, third-party hosted security products, or unrelated repository protections.
* Do not upgrade the application/toolchain or approved GitHub Action pins merely because newer versions exist.
* Do not weaken `make ci`, `make security`, or another canonical Make gate to make hosted CI pass.

## Context

Canonical sources:

* `docs/AI_WORKFLOW.md`
* `docs/delivery/implementation-plan.md`
* `docs/delivery/deployment.md`
* `docs/architecture/architecture.md`
* `docs/architecture/decisions/ADR-002-github-pages-hosting.md`
* `docs/architecture/threat-model.md`
* `docs/quality/quality-strategy.md`
* `docs/agentic/learnings/candidates/LEARN-20260923-220250-validate-playwright-native-dependencies.md`

TASK-0001 established a passing local `make ci` contract and demonstrated that installing the Playwright package/browser alone does not prove Linux browser runtime readiness. Hosted CI must explicitly provision the Playwright native dependencies before browser execution.

Current approved GitHub Action pins:

* `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1` — v7.0.1
* `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020` — v7.0.0
* `actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d` — v6.0.0
* `actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9` — v5.0.0
* `actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346` — v5.0.1

Current CI security-tool baseline:

* Gitleaks 8.30.1, Linux x64 release artifact SHA-256:
  `551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb`
* OSV-Scanner 2.6.0, Linux amd64 release artifact SHA-256:
  `ca69b3d3cd08f889a49dc0a383122f71cc528b83803671df5fd874d97485b108`

Provision these exact tools from their official release artifacts and verify checksums before execution. Do not add wrapper Actions merely to install them.

Human/platform prerequisite before deployment-path runtime verification:

* Human-confirmed 2026-09-24: the Pages configuration UI is available, and repository Settings → Pages → Build and deployment → Source is set to `GitHub Actions`.
* if this cannot be selected without changing account/organization policy, stop and return to `chatgpt-analysis`;
* do not solve Pages enablement with a PAT or broader workflow token.

## Allowed / expected scope

* Likely files/modules:

  * `.github/workflows/ci.yml`
  * `.github/workflows/deploy-pages.yml`
  * a small repository script/helper for deterministic GitHub-runner provisioning of pinned Gitleaks and OSV-Scanner, if useful to avoid duplicated shell logic
  * task/current-state/deployment documentation evidence required by the repository workflow
* Interfaces that may change:

  * GitHub Actions CI and Pages deployment surface only
  * no application runtime API or public product contract changes
* Production dependencies allowed? no
* Schema/infra/security boundary changes allowed? yes — only the already-approved GitHub Actions/Pages release trust boundary described by ADR-002 and the threat model

Use an explicit GitHub-hosted `ubuntu-24.04` x64 runner rather than relying on the moving `ubuntu-latest` alias.

## Escalation triggers

* Trigger: any approved GitHub Action SHA/tag is unavailable, does not execute on the selected runner, or proves incompatible with another required action in the complete workflow.

* Required action/profile: stop, preserve evidence, and hand off to `chatgpt-analysis` for whole-workflow compatibility reassessment; do not silently upgrade one action.

* Trigger: Gitleaks 8.30.1 or OSV-Scanner 2.6.0 cannot be reproducibly provisioned from its official Linux x64/amd64 artifact with the approved checksum.

* Required action/profile: stop and hand off to `chatgpt-analysis`; do not use an unpinned installer, `curl | sh`, or a new wrapper Action.

* Trigger: `make ci` cannot pass on a clean `ubuntu-24.04` GitHub-hosted runner after exact Node/npm restore, approved security-tool provisioning, `make bootstrap`, and Playwright native dependency provisioning.

* Required action/profile: record the concrete incompatibility and escalate to `chatgpt-analysis`; do not weaken canonical gates.

* Trigger: Pages requires a PAT, repository secret, GitHub App credential, `contents: write`, `actions: write`, administration permission, or any permission beyond the approved Pages deployment model.

* Required action/profile: stop. Human/`chatgpt-analysis` must reassess platform setup or architecture.

* Trigger: GitHub Pages is unavailable for the repository/account or cannot be configured with Source = GitHub Actions.

* Required action/profile: stop before deployment verification and hand off to human/`chatgpt-analysis`; do not change hosting target autonomously.

* Trigger: implementation would deploy from a non-`main` ref or make deployment automatic.

* Required action/profile: stop; this violates ADR-002.

* Trigger: implementation or review would need to trigger an actual production deployment to prove task correctness.

* Required action/profile: do not deploy. Record the remaining production-runtime verification as release evidence requiring a later explicit human release verdict.

## Acceptance criteria

* [x] Routine CI workflow exists and runs on pull requests targeting `main` and pushes to `main`.
* [x] Routine CI uses explicit `ubuntu-24.04`, exact approved `actions/checkout` and `actions/setup-node` SHAs, and exact Node 24.21.0/npm 11.19.0 project metadata.
* [x] Routine CI has no write permission; its effective repository permission is limited to `contents: read` unless a narrower/no-content step allows less.
* [x] CI reproducibly provisions Gitleaks 8.30.1 and OSV-Scanner 2.6.0 from official release artifacts and verifies the approved SHA-256 values before putting the binaries on `PATH`.
* [x] CI runs `make bootstrap`, provisions Chromium native Linux dependencies using the pinned project Playwright CLI, and then runs the unchanged canonical `make ci`.
* [x] A real GitHub-hosted CI run for the TASK-0002 branch/PR passes the canonical `make ci` contract, including Gitleaks, OSV-Scanner, build, and Chromium E2E.
* [x] A separate Pages workflow is triggered only by `workflow_dispatch`; push/PR/schedule triggers cannot deploy.
* [x] The Pages workflow explicitly refuses/skips deployment unless `github.ref == 'refs/heads/main'`.
* [x] The Pages build job uses explicit `ubuntu-24.04`, approved full-SHA Actions, exact project Node/npm restore, pinned security-tool provisioning, Playwright native dependency provisioning, and the unchanged `make ci` contract before Pages artifact upload.
* [x] The Pages build job has only the permissions it needs: `contents: read` and `pages: read` for checkout/configuration of this private Pages repository.
* [x] `actions/configure-pages` uses its normal read/configuration behavior only; automatic Pages enablement is not used.
* [x] Only the generated `dist/` static output is uploaded through `actions/upload-pages-artifact`; source files, repository metadata, `node_modules`, test reports, and other workspace content are not included.
* [x] The deploy job depends on the successful build/upload job and deploys that exact `github-pages` artifact rather than rebuilding.
* [x] The deploy job uses the `github-pages` environment and only `pages: write` plus `id-token: write`; it does not require repository write access or secrets.
* [x] Pages deployment concurrency prevents overlapping production deployments without cancelling an already-running production deployment.
* [x] The workflow exposes the deployed `page_url` from `actions/deploy-pages`.
* [x] Existing Vite `/MiniJSClock/` base-path behavior remains unchanged, and local/static artifact inspection confirms generated asset URLs are suitable for repository Pages hosting.
* [x] No product/runtime dependencies or application behavior are changed.
* [x] No actual production deployment is performed without a separate explicit human release verdict.

## Verification plan

* Baseline commands:

  * `git status --short`
  * `make doctor`
  * `make harness-check`
  * `make bootstrap`
  * `make ci`
* Local/static checks:

  * inspect every `uses:` entry and verify full-SHA pin plus expected release tag
  * verify Gitleaks/OSV URLs, versions, architecture, and SHA-256 checks before using the helper in CI
  * shell syntax/checks for any new provisioning helper
  * `git diff --check`
  * `make harness-check`
  * `make security`
  * `make verify`
  * `make test-e2e`
  * `make ci`
  * `make build` and inspect `dist/` for static-only output and `/MiniJSClock/` asset paths
* Hosted CI verification:

  * push the implementation branch and exercise the actual `pull_request` routine-CI trigger
  * require a clean GitHub-hosted `ubuntu-24.04` run of the canonical `make ci` path
  * inspect the run's effective setup to confirm security tools and Playwright native dependencies are provisioned rather than inherited accidentally from a local workstation
* Pages workflow verification:

  * verify GitHub recognizes the workflow without workflow syntax/configuration errors
  * verify manual-only trigger, explicit `main` guard, job dependency, artifact path/name, environment, concurrency, and job-level permissions by inspection and formal review
  * do not trigger deployment as part of implementation/review
  * record human confirmation that repository Pages source is configured as `GitHub Actions`, or leave that prerequisite explicitly open if platform policy prevents confirmation
* Security/review:

  * re-check TM-002 and TM-003 against the final diff
  * review the complete transitive workflow trust surface, not individual Actions in isolation
  * verify no new secret/PAT and no permissions beyond the approved model
  * run `make security`
  * formal fresh top-level security-focused `independent-cloud-deep` review of the task contract, actual diff, workflow permissions, SHA pins, provisioning checksums, hosted CI evidence, and deployment artifact chain

## Implementation notes

The implementation should prefer two explicit workflows:

1. routine CI for PRs/pushes, with read-only permissions and no Pages deployment authority;
2. manual Pages release workflow with a verified build job followed by a narrowly privileged deployment job.

Do not share deployment write permissions with routine CI.

The release workflow must build and verify the artifact in the same workflow run that uploads it; the deploy job consumes the uploaded artifact and must not rebuild application code.

Keep the existing local Make contract authoritative. GitHub Actions provisions the clean runner environment needed to execute that contract; it does not replace it with a parallel set of npm commands.

The Playwright native-dependency learning from TASK-0001 is deliberately applied here at the environment boundary. Do not silently promote or rewrite the learning candidate itself unless separately reviewed.

Version-sensitive compatibility is evaluated as a complete set: runner image + Action runtimes + Node/npm + package lock + Playwright browser/native libraries + security scanners + Pages permissions/artifact/deploy chain. A package/action being individually available is not sufficient evidence.

## Evidence / completion

* Files changed: `.github/workflows/ci.yml`, `.github/workflows/deploy-pages.yml`, `scripts/ci/install-security-tools.sh`, `docs/delivery/deployment.md`, this task, and `docs/current-state.md`. No application, lockfile, Make contract, or version pin changed.
* Commands run and outcomes (2026-09-24): baseline `git status --short` clean on `codex/task-0002-implementation`; `make doctor`, `make harness-check`, `make bootstrap`, and `make ci` passed before editing. After implementation, `bash -n scripts/ci/install-security-tools.sh`, `make harness-check`, `make security`, `make verify`, `make test-e2e`, `make ci`, `make build`, and `git diff --check` passed. `make security` also passed with the freshly downloaded, checksum-verified release binaries first on `PATH`.
* Handoff verification (2026-09-24): `gh pr view 18` confirmed the open PR targets `main` from the TASK-0002 branch at the stated head; `gh run view 36025339427` and its `verify` job log confirmed the hosted result below. `make harness-check` and `git diff --check` passed after this documentation update.
* Hosted CI run evidence (2026-09-24): [PR #18](https://github.com/gk79/MiniJSClock/pull/18) was verified targeting `main` from `codex/task-0002-implementation`; the tested implementation commit was `cbc596dea1140fc70504f30410880e70028b3ac2`. GitHub [CI run 36025339427, run number 12](https://github.com/gk79/MiniJSClock/actions/runs/36025339427) was triggered by `pull_request` and concluded `success`; its `verify` job completed successfully on a GitHub-hosted `ubuntu-24.04` runner. The job log confirms successful checkout, Node 24.21.0/npm 11.19.0 setup, checksum-verified Gitleaks 8.30.1 and OSV-Scanner 2.6.0 provisioning, `make bootstrap`, Playwright Chromium native dependency installation, and `make ci`. Within `make ci`, formatting, lint, type check, unit test, Gitleaks, OSV-Scanner, build, and Chromium E2E all passed. The PR event checked out GitHub's merge ref `591d6627937a964636fdbc2c01253bbf568e17a3`, which merged the stated branch head into the then-current `main`.
* Pages configuration evidence: human confirmation dated 2026-09-24 is in the task Context; read-only `gh api repos/gk79/MiniJSClock/pages` returned `build_type: workflow` and the repository Pages URL. The manual workflow has not been dispatched; no production deployment occurred.
* Workflow/action/security-tool pin verification: official GitHub release API metadata matched the task's Gitleaks 8.30.1 Linux x64 and OSV-Scanner 2.6.0 Linux amd64 SHA-256 values. The helper downloaded both official assets, verified their approved digests before extraction/execution, and the resulting binaries reported versions 8.30.1 and 2.6.0. Official Action tag refs resolved to all five task-approved full SHAs. Action metadata showed Node 24 runtimes for checkout, setup-node, configure-pages, and deploy-pages; `upload-pages-artifact` is a composite action. Static YAML inspection passed for both triggers, `main` guards, `ubuntu-24.04`, exact Node/npm checks, canonical `make ci`, all seven full-SHA `uses:` occurrences, job permissions, Pages enablement disabled, `github-pages` artifact identity, build/deploy dependency, environment URL, and non-cancelling concurrency. Hosted compatibility is now evidenced for routine PR CI; the Pages workflow remains verified by local/static inspection only.
* Static artifact/base-path evidence: `make build` produced only `dist/index.html` and generated CSS/JS files under `dist/assets/`; `dist/index.html` references both assets under `/MiniJSClock/assets/`. No source, metadata, dependency tree, test report, or symlink appeared in `dist/`.
* Implementer review status at handoff: formal independent review was still pending; implementer inspection was not a formal verdict.
* Remaining release evidence: the separate Pages workflow has not been dispatched, so hosted Pages deployment behavior remains unverified. No production release verdict has been made; deployment and hosted Pages runtime verification require a later explicit human release decision. The existing TASK-0001 Playwright native-dependency learning candidate was applied without promotion.

## Formal independent security review (2026-09-24)

**Verdict: PASS.** Fresh top-level `independent-cloud-deep` review of [PR #18](https://github.com/gk79/MiniJSClock/pull/18), branch head `4c71cf4a0af23794f7f422dd83a481437237a113`, against `origin/main` at `983c6ca104b5f1009b38af33539e7ff261460943`. No blocking findings. This is a review verdict, not a merge or production release decision.

* Hosted evidence: [CI run #13, ID 36026357337](https://github.com/gk79/MiniJSClock/actions/runs/36026357337) is the `CI` pull-request run for the current PR head and concluded `success`. Its hosted `verify` job log shows Ubuntu 24.04, effective `contents: read` plus implicit metadata read, Node 24.21.0/npm 11.19.0, checksum-verified scanner installation, `make bootstrap`, Playwright Chromium native dependency installation, and `make ci` passing formatting, lint, types, unit tests, Gitleaks, OSV-Scanner, build, and Chromium E2E.
* Independent local checks: `git diff --check origin/main...4c71cf4a0af23794f7f422dd83a481437237a113`, `bash -n scripts/ci/install-security-tools.sh`, `make harness-check`, `make security`, and `make verify` passed. Static YAML parsing/assertions passed for both triggers, both `main` guards, job permissions, runner image, concurrency, artifact identity, dependency, and all seven workflow `uses:` entries as full SHAs. Local `dist/` contained only `index.html` and generated CSS/JS in `assets/`, without symlinks; HTML asset URLs retained `/MiniJSClock/`.
* TM-002: all five direct Actions resolve to their approved official release tag SHAs. The pinned `upload-pages-artifact` composite archives only its supplied `dist/` path and invokes `actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f`, also matching official v7.0.0. The installer downloads official Gitleaks 8.30.1 and OSV-Scanner 2.6.0 Linux x64/amd64 assets and verifies their exact official SHA-256 digests before extraction/execution or adding the directory to `GITHUB_PATH`. No unpinned installer Action, `curl | sh`, PAT, repository secret, broader IAM, or hidden credential path appeared in the diff.
* TM-003: routine CI cannot deploy. Pages is manual-only and guarded to `main` in both jobs; the build job has `contents: read` and `pages: read`, disables automatic Pages enablement, completes the unchanged `make ci`, then uploads `dist/` as `github-pages`. The dependent deploy job has only `pages: write` and `id-token: write`, uses the `github-pages` environment, consumes that named artifact without rebuilding, and exposes `page_url`. Concurrency prevents overlap without cancelling an in-progress deployment. No product/runtime dependency, Make contract, or application behavior changed.
* Limit: the Pages workflow has not run or deployed. Before merge, GitHub lists only the default-branch CI workflow; the Pages workflow was checked statically, not by hosted dispatch. Its registration, hosted execution, deployed site behavior, and production release verdict belong to later human-controlled integration/release evidence. No review-time deployment occurred.
* Handoff: task complete for review; human decides whether to integrate PR #18. No new project harness learning was identified in this review.

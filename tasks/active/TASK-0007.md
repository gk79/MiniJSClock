# TASK-0007: City-local alarm configuration, domain, and persistence

- Status: in-progress
- Risk: high
- Execution profile: cloud-deep
- Reasoning target: high
- Required review: independent-cloud-deep
- Security impact: none
- Security rationale: Alarm definitions remain ordinary browser-local product configuration inside the existing client-only persistence/time trust boundary; no sensitive data, runtime network input, executable content, production permission, authentication, or new dependency boundary is introduced.
- Next handoff: cloud-deep
- Related requirements: FR-004, FR-007, FR-008, FR-010, NFR-002, NFR-003, NFR-004, NFR-005, NFR-006
- Related ADRs: ADR-001

## Context

TASK-0006 is completed/integrated with hosted CI and product-owner manual acceptance PASS. The planning baseline is synchronized `main` at `b1b32de1fef3d430313cfc1f18a7cfbc1b6d6845`. Implementation awaits planning review and a fresh primary session with a validated cloud-deep/high workstation mapping.

Canonical context: [alarm semantics](../../docs/product/requirements.md#first-release-alarm-semantics), [implementation plan](../../docs/delivery/implementation-plan.md), [ADR-001](../../docs/architecture/decisions/ADR-001-frontend-stack.md), [threat model](../../docs/architecture/threat-model.md), and [quality strategy](../../docs/quality/quality-strategy.md).

Current `src/config.ts` validates exact V1/V2, migrates V1 lazily, and protects legitimate future versions. `src/time.ts` supplies native Intl formatting/analog primitives; civil-date resolution is not implemented. `src/App.vue` centralizes city removal and persistence with one display ticker. City IDs and IANA zones come from the bundled catalog.

## Allowed / expected scope

- Likely files/modules: framework-independent alarm module(s), `src/config.ts`, relevant time primitives, `src/App.vue` and small alarm editor/card components/styles; focused unit/component tests and `e2e/` configuration/persistence tests; relevant behavior documentation and task evidence.
- Interfaces that may change: internal alarm domain/configuration types and exact browser-local Config V2 -> V3 schema, including lazy V1/V2 migration.
- Production dependencies allowed: no.
- Schema changes allowed: only the approved browser-local configuration evolution. Infrastructure, deployment, catalog, and security boundaries remain unchanged.
- Implement domain and persistence first, then configuration UI and browser regressions, with deterministic verification at each increment. No runtime alarm orchestration is authorized.

## Goal

Implement the framework-independent city-local alarm domain, alarm configuration UI, and versioned browser-local persistence for one-time and daily alarms, ready for TASK-0008 to attach runtime overdue evaluation and audible delivery.

## Non-goals

Explicitly exclude:

- audible playback;
- Web Audio/HTMLAudio orchestration;
- autoplay/user-gesture readiness;
- browser background/runtime scheduling integration;
- wiring alarm due evaluation into the application's ticker;
- production/background latency guarantees;
- notifications/service workers;
- alarms after the browser/application is closed;
- multiple alarms per city clock;
- alarms on the browser-local top clock;
- changes to city catalog/generation;
- new production dependencies;
- deployment/workflows;
- TASK-0008 implementation;
- TASK-0009 visual/release hardening.

## Expected domain model

Prefer the smallest exact model satisfying the clarified requirements.

A reasonable V3 alarm representation is conceptually:

- one-time:
  - selected city ID;
  - recurrence discriminator `once`;
  - canonical resolved UTC instant;
- daily:
  - selected city ID;
  - recurrence discriminator `daily`;
  - exact city-local `HH:mm`.

There must be at most one alarm record per selected city ID.

Do not require a separate alarm identifier unless implementation evidence shows it is actually necessary.

## Persistence evolution

Evolve exact Config V2 to exact Config V3 containing alarms.

Required migration behavior:

- valid V1 -> in-memory V3:
  - preserve selected-city order;
  - default presentation Digital;
  - default format 24h;
  - alarms empty;
- valid V2 -> in-memory V3:
  - preserve selected cities;
  - preserve presentation;
  - preserve time format;
  - alarms empty;
- loading valid V1/V2 alone must not eagerly rewrite localStorage;
- next normal configuration change may persist exact V3;
- valid V3 round-trips exactly;
- alarm city references must correspond to selected city IDs;
- alarm city IDs must be unique;
- daily `HH:mm` must be exact and valid;
- one-time resolved instant must be canonical and valid according to the task's chosen exact representation;
- malformed current documents use invalid-data recovery;
- legitimate future numeric safe-integer versions greater than current V3 remain `unsupported` and protected from overwrite;
- preserve the TASK-0006 R1 discriminator rule: malformed version types/values are `invalid`, not `unsupported`;
- storage unavailable/write-failure recovery remains unchanged.

## Domain logic

Keep alarm semantics framework-independent TypeScript where practical.

Expected pure capabilities include:

- parse/validate city-local date and minute input;
- resolve one-time city-local date/minute to candidate instant(s) using the selected city's IANA zone;
- reject nonexistent civil minutes;
- deterministically handle repeated civil minutes according to the approved first-future rule;
- calculate the daily occurrence for a city-local date;
- skip nonexistent daily civil minutes;
- select only the first occurrence in repeated time;
- evaluate alarms over actual instant interval
  `(previousExclusive, nowInclusive]`;
- return due alarms/state transition deterministically without playing sound;
- consume one-time alarms after due;
- retain daily alarms;
- identify stale one-time alarms that predate a new application session without marking them due.

Use native `Intl` / IANA behavior and deterministic injected instants.

Do not add a time-zone library or Temporal/polyfill dependency unless the approved approach proves impossible; if a new dependency or consequential time architecture becomes necessary, stop and escalate.

## UI configuration

Provide visible accessible alarm configuration on selected world-clock cards.

At minimum support:

- Set alarm when none exists;
- recurrence choice: One-time / Daily;
- city-local time input;
- city-local date input for One-time;
- Save;
- Edit/replace the existing alarm;
- Remove alarm;
- visible concise alarm summary/state on the clock card;
- understandable validation for past/nonexistent one-time civil time.

The UI saves configuration only.

It must not emit an alarm or run due-alarm orchestration in TASK-0007.

Preserve:

- global Digital/Analog settings;
- 12h/24h display preference;
- existing city picker;
- city add/remove/order;
- one shared display ticker;
- existing storage warnings.

## DST / correctness test requirements

Deterministic pure tests must cover representative cases including:

- ordinary one-time resolution;
- one-time target in the past;
- New York spring-forward nonexistent local minute;
- New York fall-back repeated local minute:
  - before first occurrence;
  - between first and second occurrence where the second remains future;
- daily normal occurrence;
- daily spring-forward skipped occurrence;
- daily fall-back first-occurrence-only semantics;
- actual-interval overdue evaluation;
- no duplicate daily event from repeated hour;
- long delayed interval returns at most one due event per alarm;
- one-time consumption;
- daily retention;
- expired one-time on new-session boundary is not retroactively due;
- representative non-hour offset zone such as Asia/Kathmandu.

## Persistence/UI tests

Require tests for:

- V1 -> V3 lazy migration;
- V2 -> V3 lazy migration;
- exact V3 round-trip;
- malformed V3 recovery;
- future-version non-overwrite;
- TASK-0006 malformed-version discriminator behavior retained;
- alarm references/uniqueness validation;
- city removal also removes its alarm;
- alarm add/edit/remove persistence;
- reload restores alarm configuration;
- existing world-clock/settings configuration is preserved.

## Browser scope

Browser/E2E should prove configuration and persistence under the production `/MiniJSClock/` base path.

Do not attempt to prove audio/background delivery in TASK-0007.

TASK-0008 will own audible runtime orchestration and browser-policy evidence.

## Escalation triggers

Stop instead of broadening scope if:

- native Intl/IANA cannot provide deterministic civil-time resolution with acceptable complexity;
- a new runtime time-zone/Temporal dependency appears necessary;
- requirements would need multiple alarms per clock;
- alarms need to survive city removal;
- local top-clock alarms become required;
- persistence migration cannot preserve valid V1/V2 data;
- background/audio/autoplay mechanics become necessary to satisfy TASK-0007;
- service workers/notifications/backend/runtime network behavior appear necessary;
- a security/trust boundary changes;
- verification of DST/overdue semantics cannot be made deterministic.

## Verification plan

Implementation verification requires:

Baseline:

- clean worktree;
- `make harness-check`;
- current focused config/time/App tests;
- current world/settings E2E.

Implementation:

- deterministic alarm-domain unit tests;
- persistence/migration tests;
- Vue/component tests for alarm controls/config state where useful;
- browser E2E for alarm create/edit/remove/reload and city-removal cleanup;
- existing clock/settings/city regressions;
- `make test-integration`;
- `make test-e2e`;
- `make security`;
- `make verify`;
- plain `make ci`;
- `git diff --check`.

Formal review:

- fresh top-level `independent-cloud-deep`;
- review actual task, requirements clarification, diff, DST/domain logic, persistence migrations, tests and evidence;
- Method Guide chat does not satisfy the formal review gate.

## Acceptance criteria

- [ ] Pure domain logic satisfies the approved one-time, daily, DST, actual-interval overdue, consumption/retention, and new-session stale semantics with deterministic injected instants.
- [ ] Exact V3 validation and lazy V1/V2 migration preserve valid existing configuration and all recovery/future-version protection behavior.
- [ ] Each selected world-city card supports accessible alarm create/edit/remove with concise summary and validation; at most one alarm per city, atomic city-removal cleanup, and no resurrection on re-add.
- [ ] Production-base-path configuration/persistence E2E and existing settings/city/display regressions pass; no due evaluation is wired into the ticker and no sound is emitted.
- [ ] Documentation/evidence are updated; all required gates and fresh top-level independent-cloud-deep review pass before task closure.

## Implementation notes

No implementation performed during planning. No new ADR is required by current evidence. Escalation triggers above require stopping and recording the blocker rather than broadening scope; general AI_WORKFLOW escalation rules also apply.

## Evidence / completion

- Planning files changed: `docs/product/requirements.md`, `docs/current-state.md`, `tasks/active/TASK-0007.md` only.
- Baseline: clean synchronized main at the expected SHA; `make harness-check` PASS; focused config/time/App Vitest PASS (3 files, 27 tests).
- Planning verification: `make harness-check`, `git diff --check`, and staged diff whitespace check PASS; full three-file diff inspected against main.
- Runtime evidence: no alarm runtime behavior implemented or claimed.
- Reviewer verdict: planning review pending; formal implementation review not performed.
- Remaining risks/follow-ups: native Intl civil-time resolution and deterministic DST/overdue tests require implementation evidence; audio/session/ticker integration belongs to TASK-0008, which is not created or started. Pages remains undeployed; no production release verdict.
- Harness retrospective: no new harness learning; the observed sandbox startup failure is already covered by the existing tooling candidate. Existing learning candidates remain unchanged.
- Handoff completed to: planning reviewer; cloud-deep implementation awaits planning review.

### Implementation session

- User confirmed read-only planning review complete and authorized implementation from exact remote planning head `89b592fc03142467a1248ec0ef052126a196a841`.
- Fetched remotes; exact planning head matched; checkout was clean; created local `codex/task-0007-implementation` from that head.
- Validated Layer 0 mapping and configured high reasoning for the required cloud-deep primary session.
- Baseline: `make harness-check` passed; focused config/time/App tests passed (27 tests); existing world/settings E2E passed (9 Chromium tests).
- Sandbox command startup fails on the existing WSL mount condition. Authorized commands run through the escalation path; no project write path outside this checkout.
- Domain tests first failed on the missing module, then 28 deterministic alarm tests passed after implementation. Candidate resolution exhaustively validates canonical UTC minutes against native Intl civil components; no assumed DST offset or browser-local parsing.
- V3 persistence tests first failed against V2, then passed (41 config tests). Existing App tests passed (5); lint/type/build passed after correcting a test fixture excess-property error. Existing world/settings browser regressions passed (9). Existing assertions now expect exact V3 writes and legitimate future version 4; exact-second assertions remain unchanged.

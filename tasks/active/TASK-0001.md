# TASK-0001: Bootstrap Vue/Vite repository and local verification harness

- Status: review
- Risk: medium
- Execution profile: cloud-standard
- Reasoning target: medium
- Required review: independent-cloud-standard
- Security impact: none
- Security rationale: This task materializes already-approved application/tooling dependencies and local verification commands without changing authentication, secrets, production permissions, trust boundaries, or deployment IAM; dependency changes still require explicit diff review and `make security`.
- Next handoff: independent-cloud-standard
- Related requirements: NFR-003, NFR-006, NFR-007
- Related ADRs: ADR-001

## Goal

Materialize the approved Vue + TypeScript + Vite stack in the existing Agentic SDLC repository so the repository has a minimal runnable application skeleton and deterministic local bootstrap, format, lint, type-check, unit-test, browser-smoke, security, build, verify, CI-contract, and run commands.

## Non-goals

- Do not implement world-clock product features, city catalog generation, persistence/domain behavior, alarms, or final visual design.
- Do not add Vue Router, Pinia, Tailwind, a component/UI framework, backend code, or runtime external APIs.
- Do not configure or enable the GitHub Pages deployment workflow in this task; deployment workflow permissions/release mechanics are a separate task.
- Do not replace or weaken the Agentic SDLC repository contract, `AGENTS.md`, `docs/AI_WORKFLOW.md`, task lifecycle, security baseline, or canonical Make target interface.
- Do not upgrade the approved architecture/toolchain versions merely because newer versions exist; use the compatibility baseline revised on 2026-09-23 and revalidate/escalate only if an approved pin cannot be used safely.

## Context

Canonical sources:
- `docs/architecture/architecture.md`
- `docs/architecture/decisions/ADR-001-frontend-stack.md`
- `docs/quality/quality-strategy.md`
- `docs/architecture/threat-model.md`
- `docs/agentic/project-capabilities.md`
- `.agents/skills/repo-bootstrap/SKILL.md`

Layer 0 was validated before this task was created. The approved project-specific editor capability is `Vue.volar`.

Current implementation planning has one human contributor and no concurrent human implementation of other tasks; therefore no explicit multi-human execution DAG is required for TASK-0001.

## Allowed / expected scope

- Likely files/modules:
  - `package.json`, `package-lock.json`, `.nvmrc`
  - Vue/Vite/TypeScript scaffold files under the repository root and `src/`
  - minimal test configuration/files for Vitest, Vue Test Utils where useful, and Playwright
  - `vite.config.*`, `tsconfig*.json`, lint/format configuration required by the chosen official scaffold/tooling
  - `scripts/commands/bootstrap.sh`
  - `scripts/commands/format-check.sh`
  - `scripts/commands/lint.sh`
  - `scripts/commands/typecheck.sh`
  - `scripts/commands/test.sh`
  - `scripts/commands/test-integration.sh`
  - `scripts/commands/test-e2e.sh`
  - `scripts/commands/build.sh`
  - `scripts/commands/run.sh`
  - `.vscode/extensions.json`
  - minimal documentation/current-state/task evidence required by the repository workflow
- Interfaces that may change:
  - stack-specific implementations behind the existing canonical `make` targets
  - package scripts and application entry point
- Production dependencies allowed? yes — only dependencies already approved by ADR-001/architecture; any additional scaffold/tooling dependency must be necessary for the approved verification contract, explicitly pinned, and reviewed in the diff
- Schema/infra/security boundary changes allowed? no

## Escalation triggers

- Trigger: an approved direct version pin is unavailable, incompatible, or has a material security issue.
- Required action/profile: stop, record evidence, and hand off to `chatgpt-analysis` for architecture/toolchain reassessment.

- Trigger: satisfying lint/format/test/build requires a materially different framework, package manager, state library, UI library, router, backend, or deployment model.
- Required action/profile: stop and hand off to `chatgpt-analysis`; do not broaden the stack.

- Trigger: implementation requires GitHub Actions permissions, Pages environment configuration, secrets, production IAM, or other deployment trust-boundary work.
- Required action/profile: stop; defer to the separate deployment task, expected to use a stronger security-focused review contract.

- Trigger: canonical repository commands cannot be made deterministic in the target environment.
- Required action/profile: record the blocker and escalate to `chatgpt-analysis` rather than bypassing the command contract.

## Acceptance criteria

- [x] Node/npm baseline is materialized as approved: `.nvmrc` uses Node 24.21.0; `package.json` records the Node 24 engine constraint and `"packageManager": "npm@11.19.0"`.
- [x] Direct dependencies are pinned exactly and `package-lock.json` is committed; approved core pins from architecture are preserved, including `typescript@6.0.3`, `vue-tsc@3.3.11`, `eslint@10.10.0`, `@vue/eslint-config-typescript@14.9.0`, and `eslint-plugin-vue@10.11.0`.
- [x] A minimal Vue 3 + TypeScript + Vite application runs without Router, Pinia, Tailwind, a component framework, backend code, or runtime API dependency.
- [x] Vite production configuration supports the GitHub Pages repository base path `/MiniJSClock/`.
- [x] The scaffold contains only a minimal product shell/smoke surface; no world-clock feature implementation is pulled into bootstrap.
- [x] `make bootstrap`, `make format-check`, `make lint`, `make typecheck`, `make test`, `make security`, `make build`, `make verify`, `make ci`, and `make run` have deterministic stack-specific behavior.
- [x] `make test-e2e` runs a minimal Playwright browser smoke check against the application and verifies that the root application surface renders.
- [x] If `make test-integration` has no meaningful integration boundary yet, it returns a successful explicit `NOT APPLICABLE: <reason>` result rather than remaining a placeholder.
- [x] No `scripts/commands/*.sh` target needed by the selected stack remains in the starter's `NOT CONFIGURED` state.
- [x] `.vscode/extensions.json` recommends `Vue.volar`; when the executor controls the developer workstation, install/validate it, otherwise record that local installation remains a human workstation action.
- [x] The production build contains static assets only and does not require a runtime server/backend.
- [x] Existing Agentic SDLC documentation/tasks/harness files are preserved unless a task-required factual update is necessary.

## Verification plan

- Baseline command(s):
  - `git status --short`
  - `make doctor`
  - `make harness-check`
- Required automated checks:
  - clean dependency restore using the committed lockfile after it exists
  - `make format-check`
  - `make lint`
  - `make typecheck`
  - `make test`
  - `make security`
  - `make build`
  - `make verify`
  - `make ci`
  - `make test-e2e`
  - `make test-integration` (real checks if applicable, otherwise explicit successful N/A adapter)
- Runtime/manual scenario:
  - `make run` starts the development application and the minimal application shell renders in a browser.
  - verify a production build can be served under a `/MiniJSClock/` base-path scenario.
  - if possible in the execution environment, validate `Vue.volar` is installed/active; otherwise record the environment limitation.
- Security/review checks:
  - review all dependency additions and lockfile changes.
  - `make security` must pass with Gitleaks and OSV-Scanner.
  - formal fresh top-level `independent-cloud-standard` review after implementation; reviewer checks the task contract, actual diff, lockfile/dependencies, harness behavior, and verification evidence.

## Implementation notes

Use the repository-local `repo-bootstrap` skill. Integrate the official Vue scaffold into the existing repository carefully; do not initialize a new repository or overwrite the Agentic SDLC durable artifacts.

Keep domain/product behavior out of the bootstrap. The purpose of this task is to make later vertical feature tasks cheap and objectively verifiable.

### Prior blocked attempt evidence

The first primary implementation session correctly stopped on the version-compatibility escalation trigger. Its durable handoff is synchronized at `codex/task-0001-blocked-handoff`, commit `e7f9f43a897b6d733688a9280e68432547910fe6`.

Verified evidence from that attempt:

- Node 24.21.0 and npm 11.19.0 were checksum-verified and installed successfully.
- The approved `typescript@7.0.2` pin caused `npm run lint` to fail because the current TypeScript-ESLint line does not support TypeScript 7.
- `npm run type-check` failed because `vue-tsc@3.3.11` could not load the TypeScript 7 compiler path.
- `npm ci --ignore-scripts` completed with zero npm-audit vulnerabilities but reported the corresponding peer conflicts.
- The implementer did not downgrade an approved pin or bypass lint/type-check gates.
- `make harness-check` and `git diff --check` passed at handoff.

Approved recovery:

- use `typescript@6.0.3`;
- retain `vue-tsc@3.3.11`;
- retain the already-materialized lint baseline `eslint@10.10.0`, `@vue/eslint-config-typescript@14.9.0`, and `eslint-plugin-vue@10.11.0`;
- regenerate/reconcile the lockfile and require a clean install without the prior TypeScript peer conflicts;
- resume in a **fresh** primary `cloud-standard` session.

The handoff branch is evidence/reference, not the integration target. Start implementation from the latest `main` after this compatibility decision is integrated. Reuse partial scaffold files from the handoff branch selectively when useful; do not merge its stale blocked task/current-state wholesale.


## Evidence / completion

- Files changed: exact npm/toolchain metadata and lockfile; minimal Vue entry point/shell/styles; Vitest and Playwright smoke tests/configuration; Vite/TypeScript/ESLint/Prettier configuration; canonical command adapters and CI composition; Volar recommendation; task/current-state evidence.
- Commands run and outcomes: baseline `make doctor` and `make harness-check` passed; `npm install` and canonical `make bootstrap` completed with zero npm-audit vulnerabilities and no peer conflicts; the unit test first failed because `App.vue` was absent, then passed after the shell was implemented; `make format-check`, `make lint`, `make typecheck`, `make test`, `make test-integration` (explicit N/A), `make security`, `make build`, `make verify`, `git diff --check`, and dependency-pin review passed. After the human provisioned Playwright Chromium's Linux system dependencies, `make bootstrap`, `make test-e2e`, and `make ci` all passed; the CI composition included format, lint, type-check, unit, Gitleaks, OSV-Scanner, build, integration N/A, and Chromium E2E gates.
- Runtime evidence: `make run` started Vite 8.3.0 at `http://localhost:5173/MiniJSClock/`; an HTTP request to the configured base path returned the application HTML. The production build contains only `dist/index.html` plus generated CSS/JavaScript assets, and Playwright Chromium rendered the application shell successfully from the `/MiniJSClock/` production preview path.
- Reviewer verdict: not requested; formal `independent-cloud-standard` review remains pending, so the task stays active in `review` and is not `done`.
- Remaining risks/follow-ups: The `code` CLI did not report `Vue.volar` as installed, so extension installation/activation remains a human workstation action. Harness candidate `LEARN-20260923-220250` proposes deterministic preflight detection for Playwright's native-library prerequisite. Harness retrospective at verification handoff found no additional harness learning beyond that existing candidate.
- Handoff completed to: `independent-cloud-standard`. Recommended next action: perform the required fresh top-level review of the task contract, actual diff, dependency/lockfile changes, harness behavior, and recorded verification evidence.
